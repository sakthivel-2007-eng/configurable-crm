# pnpm settings file (pnpm >= 10 reads settings from here, not package.json).
# esbuild ships a platform binary via its install script; Vite cannot start
# without it, so it is explicitly allowed to run.
allowBuilds:
  esbuild: true
